import { ExceptionErrorFilter } from './exception-error.filter';

describe('ExceptionErrorFilter', () => {
  it('should be defined', () => {
    expect(new ExceptionErrorFilter()).toBeDefined();
  });
});
